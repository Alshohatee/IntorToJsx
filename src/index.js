// let React = require("react");
// let ReactDOM = require("react-dom");
import react from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  // only one element to render
  <div>
    <h1>Hello World!</h1>
    <p> This is a paragraph</p>
  </div>,
  document.getElementById("root")
);
